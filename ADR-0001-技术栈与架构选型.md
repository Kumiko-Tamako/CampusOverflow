# ADR-0001：技术栈与架构选型

- 状态：**已接受**
- 日期：2026-09-10
- 决策者：全体成员（SM 归档）
- 建议存放：`docs/adr/0001-技术栈与架构选型.md`

## 背景

CampusOverflow 校园问答知识社区需要支撑教学答疑沉淀与学习参与量化。团队需在迭代早期确定技术栈与代码组织方式，以保证 2 名 Developer（前后端各一）并行开发、契约衔接清晰、质量门禁可执行。

## 决策

1. **语言与框架**：后端 Python 3.12+ / FastAPI + Uvicorn；前端 React 18 + TypeScript 5（strict，禁止 any）+ Vite。
2. **数据与存储**：PostgreSQL 16（SQLAlchemy 2.0 async + Alembic 迁移）、Redis 7（缓存/Broker）、Celery 异步任务。
3. **限界上下文纵切**：按 5 个子域分包——identity / qa / course / reputation / discovery，每个上下文内再分 domain / application / infrastructure / interfaces 四层。
4. **单体 + 模块化**：单进程单体；模块间经应用层协作，不跨上下文直连他域仓储。
5. **认证与安全**：JWT 双令牌（Access 15min + Refresh 7d 存 Redis 可吊销），RBAC 角色权限；密码 bcrypt 哈希。
6. **主键与聚合**：UUID 主键；聚合刻意收小（如 Question 聚合仅含 Question + Vote），事务边界清晰优先。
7. **契约衔接**：后端产出 `openapi.json` 为唯一契约物，前端 openapi-typescript-codegen 拉取生成客户端。
8. **质量门禁**：ruff + mypy --strict + 覆盖率（整体 ≥ 70%、核心聚合 ≥ 90%）+ 队友评审通过，缺一不可。

## 后果

- 收益：前后端通过 openapi.json 并行开发互不等待；门禁前置使合并质量可预期；DDD 分层为后续 reputation/course/discovery 扩展留出结构。
- 成本：单体需严格自律避免跨上下文直连；JWT 双令牌需配套吊销与轮换机制；mypy --strict 对类型注解要求较高，初期有学习成本。

## 替代方案

- 微服务拆分：当前团队 4 人、2 Dev，微服务运维成本过高，否决。
- 前后端混用非契约文档对接：易产生接口漂移，否决。
